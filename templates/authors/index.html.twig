<!DOCTYPE html>
<html lang="EN">
<head>
    <title>FouX Blog</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="mt-5 container">
    <header>
        <div class="d-flex">
            <div class="vw-100"><h1 class="alert alert-info"><b>Authors List</b></h1></div>
            <div class="m-2 mt-0"><a class="btn btn-outline-danger" href="{{ path('app_authors_login') }}">Login</a> <a class="btn btn-outline-warning mt-1" href="{{ path('app_authors_register') }}">Register</a></div>
        </div>
    </header>

    <!-- Post List -->
    <div class="row">
        {% for author in authors %}
            <div class="col-md-4 mt-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ author.username }}</h5>
                        <p class="card-text">{{ author.bio }}</p>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <p>There is no Author</p>
            </div>
        {% endfor %}
    </div>
</div>
</body>
</html>


