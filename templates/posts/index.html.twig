<!DOCTYPE html>
<html lang="EN">
<head>
    <title>FouX Blog</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="mt-5 container">
    <header id="FouXBlog">
        <div class="mt-5 container">
            <h1><b>FouX Blog</b></h1>
        </div>
    </header>

    <!-- Post List -->
    <div class="row">
        {% for post in posts %}
            <div class="col-md-4 mt-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">{{ post.title }}</h5>
                        <p class="card-text">{{ post.description|slice(0, 100) ~ '...' }}</p>
                        <a href="{{ path('app_posts_show', { 'id': post.id }) }}" class="btn btn-sm btn-outline-secondary">Read More..</a>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <p>No posts found</p>
            </div>
        {% endfor %}
    </div>
</div>
</body>
</html>


